meta {
  name: WellnessWave API
}

docs {
  Bruno collection for the WellnessWave backend API.
  Use the Local environment before sending requests.
  Protected endpoints expect authToken to be populated from register, login, or google login.
  Update productId, categoryName, and imagePath variables as needed for parameterized requests.
}